import React, { Component } from 'react'

class LoginInput extends Component {
    render() {
        return (
            <input placeholder="Login" defaultValue="Login" />
        );
    }
}

export default LoginInput;
